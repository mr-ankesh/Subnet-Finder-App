# 2026-08-03

## Work Completed

- Built the **AI Architecture Advisor V1 (Storage only)** end-to-end, per a
  fully-specified user request: a checked-in knowledge base at `advisor_kb/`
  (5 catalog patterns, a question bank, a deterministic decision matrix, a
  request-field mapping, a recommendation template with 3 system prompts,
  and Mermaid diagram templates), all provenance-tracked to two Microsoft/
  Kyndryl design documents.
- Went through Plan Mode first per the user's explicit "plan, wait for OK,
  then implement in stages" instruction — read the entire KB before writing
  any code, forked a research pass over existing conventions
  (`agent_requester.py`'s LLM-client pattern, `chats.py`'s persistence
  shape, the storage validation chokepoint, nav/vendor precedent), then
  wrote and got approval on a staged plan.
- Implemented in 6 stages, each independently verified before moving on:
  1. `advisor/catalog_loader.py` (loads + validates the KB, fails loudly on
     malformed YAML — verified with deliberately broken test fixtures) +
     `advisor/session_store.py` (new `advisor_sessions` table, own
     `ensure_table()`, added to `scripts/sqlite_to_postgres.py`).
  2. `advisor/condition_eval.py` (a small restricted-`eval()` evaluator for
     the KB's `when`/`skip_if` condition language — found and fixed 3 real
     bugs against the KB's *actual* condition strings: re-quoting
     identifiers already inside string literals, lowercase `true`/`false`
     not converting to Python booleans, and dotted attribute access
     (`derived.lifecycle_to_archive`) being incorrectly quoted token-by-token),
     `advisor/question_engine.py`, `advisor/rules_engine.py`,
     `advisor/pattern_matcher.py` — all pure logic, verified via a new
     assert-based `scripts/test_advisor_validation.py` against the real KB
     content (not mocks), covering 8 of the user's 13 verification items
     directly.
  3. `advisor/prefill.py` + `advisor/diagram_builder.py` — found 4 more real
     mismatches between the KB's semantic vocabulary and this app's actual
     form markup (read `templates/requester.html` directly rather than
     assuming): `identity_type`/`encryption_type` values needed translating
     to the form's actual `<option>` values; `storage_premium_temporary`'s
     `Premium_ZRS` SKU had no matching form option at all (fixed — added to
     `config.py`'s `STORAGE_SKUS` and the form dropdown, a genuine
     pre-existing gap this surfaced); the KB's `ServiceClass` tag mapping
     doesn't match the form's actual options at all (left for the user to
     pick, per the KB's own hedge that this mapping is "inferred, not
     quoted").
  4. `advisor/prompts.py` (single-turn LLM call, no tool loop — unlike
     `agent_requester.py`) + `advisor/recommendation.py` (assembles the
     fixed-section-order recommendation; only "Why this pattern" is
     genuinely LLM prose, with a deterministic fallback) + 3 new `app.py`
     routes + the `requester_page()` extension for the prefill handoff.
     Verified via Flask's test client end-to-end, including a real
     unplanned LLM failure (expired provider license, 403) that correctly
     fell back to the deterministic recommendation rather than erroring.
  5. `templates/advisor.html` (chat UI, answer review/correct step,
     recommendation rendering, diagram request) + `templates/requester.html`
     prefill JS (generic `[data-detail]` population, no hand-written
     per-field mapping) + `templates/base.html` nav (both the admin
     Operations dropdown and the non-admin requester nav branch) + vendored
     Mermaid. Found and fixed a real client-side logic bug during review:
     the "Change answer" button called the server with no question id,
     which just returns whatever's next rather than re-asking the changed
     question — fixed by caching the full question object client-side.
  6. This update.
- Vendored Mermaid as the single-file UMD build (`mermaid.min.js`, 3.3 MB),
  not the ESM build the KB's own diagram README shows — the ESM entry file
  turned out to be a 76-byte re-export pointing at 121 separate hash-named
  chunk files, too fragile to vendor reliably for one feature.
- Full test suite run at the end: `test_advisor_validation.py` (63 checks),
  `test_storage_validation.py` (31), `test_resourcegraph_validation.py`
  (36) — all passing, no regressions from the `Premium_ZRS`/config.py
  changes.

## Files Changed

- New: `advisor/__init__.py`, `catalog_loader.py`, `condition_eval.py`,
  `question_engine.py`, `rules_engine.py`, `pattern_matcher.py`,
  `prefill.py`, `diagram_builder.py`, `session_store.py`, `prompts.py`,
  `recommendation.py`; `templates/advisor.html`;
  `scripts/test_advisor_validation.py`; `static/vendor/mermaid.min.js`
- Modified: `app.py` (4 new routes, `requester_page()` extended),
  `templates/requester.html` (prefill JS), `templates/base.html` (nav,
  both branches), `config.py` (`Premium_ZRS` added to `STORAGE_SKUS`),
  `requirements.txt` (`PyYAML`), `scripts/sqlite_to_postgres.py`
  (`advisor_sessions` added), `CLAUDE.md`, `docs/HOW_IT_WORKS.md`

## Design Decisions

See `.memory/architecture-decisions.md` (2026-08-03 entries): "rules decide,
LLM explains" as the enforced architecture (not just a slogan — traced to
where each guardrail is actually implemented); `advisor_sessions` as a new
table rather than reusing `chats.py`; the Mermaid UMD-over-ESM vendoring
choice; the KB-vs-real-form mismatches found and how each was resolved.

## Bugs Fixed (caught during this session's own implementation, before commit)

- `condition_eval.py`: 3 bugs in the condition-string rewriter (string-literal
  re-quoting, lowercase boolean words, dotted-attribute-access quoting) —
  all caught by testing against the KB's actual condition strings, not
  synthetic ones.
- `prefill.py`: the `service_class` field was silently never flagged as
  unfillable (a dead code path that patched a KB list entry that doesn't
  exist) — fixed to always append an explicit checklist entry.
- `templates/advisor.html`: "Change answer" would silently skip past the
  question being changed instead of re-asking it (the server has no
  "re-ask" concept — an answered question is just skipped by
  `next_question()`) — fixed by caching the full question object
  client-side instead of re-querying the server.
- `config.py`/`templates/requester.html`: `Premium_ZRS` was missing from
  `STORAGE_SKUS` and the form's SKU dropdown entirely — `storage_premium_temporary`'s
  recommended SKU had no way to actually be selected/validated. Fixed by
  widening the allowed set (not weakening `_validate_storage_request`).

## Blockers

- None. The advisor's own LLM calls degrade gracefully when the configured
  provider is unavailable (verified live, not just in theory).

## Next Steps

- Commit this feature (staged, per the user's own instruction).
- See `.memory/next-actions.md` for follow-ups (real in-browser visual
  check — no headless-browser tool was reached for in this session; a real
  Reader-only... n/a here, this feature makes no Azure calls at all).

---

## Session 2 (later same day)

### Work Completed

- **VM_CREATE re-verification, requested before a commit.** Logged in as
  local admin, submitted a real `vm_create` request via the requester
  endpoint, ran `vm_check`/`vm_deploy`. First deploy attempt actually hit
  real Azure (`dry_run: false`) despite `.env` saying `AZURE_DRY_RUN=true` —
  a **DB-level Settings override of `false` wins over the env var**
  (`config.py`'s DB → env → default resolution order). Flagged this to the
  user immediately, flipped the DB override to `true` via the real
  `/api/admin/settings` endpoint (not raw SQL), and re-ran a clean 2-VM
  dry-run request that behaved correctly end-to-end. The earlier real VM
  had already had an attempted UI revert that itself no-op'd (it ran after
  dry-run was back on, so `delete_vm` simulated instead of deleting for
  real) — cleaned up via a direct `azure_tools.delete_vm()` call with
  dry-run genuinely off, with an audit-trail note explaining the ledger's
  "reverted" status didn't reflect reality until that manual cleanup. See
  `current-state.md`'s Verification Notes and `next-actions.md` item 12 for
  the leftover resource-group cleanup this left behind.
- **Committed the AI Architecture Advisor V1 (Storage only)** from Session 1
  above, plus a small cleanup of duplicate nested `advisor_kb/` directories
  the user identified and asked to be cleaned up (`f06285c`).
- **Expanded the AI Architecture Advisor from storage-only to six services**
  (Storage/AKS/VM/Postgres/AppGW selectable, Key Vault reference-only),
  per a fully-specified user request. Read `advisor_kb/MIGRATION.md` and
  every new KB file in full before planning; entered Plan Mode, asked one
  clarifying question (Key Vault's menu status — user chose reference-only),
  got the plan approved, then implemented in 3 staged commits:
  1. `1aca22a` — service-aware catalog loading (`SERVICE_FILES`,
     `platform_constants`, `design.inherits` merge), `advisor/services.py`
     (service-selection menu + keyword fallback), question-schema
     normalization (two `options`/`skip_if` shapes now coexist),
     `rules_engine`'s generic `execution_order` iteration.
  2. `cc1e58c` — `build_prefill_aks`/`build_prefill_vm`, new diagram
     placeholders (`{BASE}`/`{BACKEND}`/`{HOSTNAME}`) + a GPU-node-pool
     diagram strip rule, `recommendation.build_recommendation_generic()`
     (security_floor as the settings table, raw `design` dict passed
     through — AKS/VM/Postgres/AppGW's design shapes are too heterogeneous
     for storage's flat table).
  3. `1c70a91` — `build_prefill_postgres`/`build_prefill_appgw` (targeting
     `RequestType.OTHER`, composed description), the `redirect` response
     type (Postgres self_managed → vm_workload_standard), full `app.py`
     wiring (service-aware `/api/advisor/chat`), and the frontend changes
     (`templates/advisor.html`'s new response type + verbatim message_ref
     rendering + generic design-dict renderer; `templates/requester.html`'s
     prefill JS reading `request_type` from the payload generically).
  4. Docs + memory (this entry) — `CLAUDE.md`, `docs/HOW_IT_WORKS.md`,
     `.memory/current-state.md`/`next-actions.md`/`architecture-decisions.md`.
- **Real bug found and fixed**: `condition_eval.evaluate()` was silently
  returning `True` (not raising) for plain-English condition strings with
  no operator at all (`"egress_destinations specified"`) — Python's
  implicit adjacent-string-literal concatenation made the rewritten
  expression a truthy non-empty string instead of failing. Fixed by
  rejecting any rewritten condition with no recognizable operator; verified
  safe against all 92 unique condition strings in the whole KB. See
  `architecture-decisions.md`'s 2026-08-03 "expanded to six services" entry
  for the full writeup and every other decision made during this build
  (service-id-vs-RequestType distinction, Key Vault reference-only,
  Postgres/AppGW → `RequestType.OTHER`, add_service/redirect semantics).
- **Verified live against the running app**, not just the 172-check
  assert suite (up from 63): a full AKS conversation through diagram render
  and prefill handoff (confirmed `project`/`env`/`tier`/`node_count`/
  `cmk_encryption` all populated correctly under the real form's field
  names); a Postgres `self_managed` conversation confirming the redirect to
  `vm_workload_standard` fires with no fabricated VM prefill; an AppGW
  public-exposure conversation confirming the InfoSec gate escalation
  renders its `message_ref` content verbatim and `request_type: "other"`
  threads through to the prefill payload correctly.

### Files Changed (six-service expansion, on top of Session 1's advisor files)

- New: `advisor/services.py`
- Modified: `advisor/catalog_loader.py`, `condition_eval.py`,
  `question_engine.py`, `rules_engine.py`, `prefill.py`, `diagram_builder.py`,
  `recommendation.py`; `app.py`; `templates/advisor.html`,
  `templates/requester.html`; `scripts/test_advisor_validation.py`
  (63 → 172 checks); `CLAUDE.md`, `docs/HOW_IT_WORKS.md`

### Blockers

- None. All five selectable services verified working, both via the
  assert suite and live against the running dev server.

### Next Steps

- See `.memory/next-actions.md` (updated) for follow-ups: real in-browser
  visual pass across all five services, dedicated
  `RequestType.POSTGRES_CREATE`/`APP_GATEWAY` types, `ServiceClass`
  vocabulary resolution, the Phase 3 environment composer, and the
  unrelated leftover items (sandbox RG cleanup, `helm/` secret-name
  confirmation, `page-bg-original.jpg`).
- `graphify update .` after this session's changes.

## Session 3 (later same day) — CNI Overlay KB correction (`ce7874b`)

- User asked to make the Advisor recommend **Azure CNI Overlay** for all
  AKS patterns. Checked `config.py`/`templates/requester.html` first and
  found the real app **already defaults to Overlay**
  (`AKS_NETWORK_PLUGIN_MODE=overlay`, `AKS_POD_CIDR=10.244.0.0/16`) — the
  advisor's KB was the one out of date, still describing classic
  VNET-integrated CNI and warning about per-pod VNET IP exhaustion (a
  problem Overlay already solves). Updated `aks_private_standard.yaml`
  (design/summary/prerequisites), `aks_decision_matrix.yaml`'s `cni_sizing`
  warning, and `aks_request_mapping.yaml`'s `locked_fields`
  (`network_plugin_mode: overlay`) — sourced against the app's own live
  Settings default rather than the pattern's two external cited documents,
  since neither speaks to Overlay; noted honestly in the pattern's own
  `source` list instead of fabricating a citation. `aks_gpu_nodepool.yaml`
  picked up the fix automatically via `design.inherits`.
- **Real bug found and fixed while verifying this live**: `advisor/prompts.py`'s
  LLM client had no read timeout on the Anthropic/Azure-OpenAI paths, and
  only a 60s timeout *with default retries* on the OpenAI-compatible path
  (the one actually configured here, `AGENT_PROVIDER=byom` pointing at an
  internal-only endpoint unreachable from this sandbox) — retries meant a
  real request could take 60-180s to fall back instead of failing fast,
  undermining "AI enhances, never gates." All three paths now use a 15s
  timeout with `max_retries=0`.
- Verified live against the running app (confirmed `design.network_plugin
  == "Azure CNI Overlay"` and the corrected warning render correctly in a
  real recommendation) and via the assert suite (175 checks, +3 for this
  change). Fallback now resolves in ~16s instead of 60-90s+.

## Session 4 (later same day) — composer KB Overlay correction (`7bf8459`)

- User gave an exact, literal patch spec for three `advisor_kb/composer/`
  files still carrying classic-CNI assumptions (`composition_rules.yaml`,
  `environment_questions.yaml`, `templates/environment_recommendation_
  template.md`) — `network_sizing.yaml`/`worked_example.md` had already
  been replaced (KB 2.1.0) by other means and needed no correction.
  Replaced the single `aks_cni_sizing` warning with three
  (`aks_overlay_sizing`, `aks_surge_headroom`, `pod_cidr_non_overlap`),
  added the Pod CIDR line to `network_plan.outputs`, corrected `aks_scale`'s
  `why_we_ask`/`if_unknown` text, added the Pod-CIDR hard rules to the
  environment recommendation template — all exact text as specified.
- Verified: both YAML files parse; `grep -r aks_cni_sizing` finds only the
  new changelog's own historical mention; `grep -ri "per pod"` finds
  nothing outside `do_not_reintroduce` notes; `environment_warnings` has
  exactly 3 AKS-gated entries. Existing 175-check suite unaffected (no
  runtime code reads `composer/` yet at this point). Reported and stopped
  per instruction — did not start Phase 3 in this session.
- `network_sizing.yaml`/`worked_example.md` were left uncommitted at the
  end of this session (modified by other means, not verified by this
  session's own edits) — committed in Session 5 below once the user
  explicitly asked for it.

## Session 5 (later same day) — Phase 3: the environment composer (`2298611`…`7e67cae`)

Built the "whole environment" composer end to end, in 5 staged commits per
the user's plan-first-then-implement instruction (plan approved via
`ExitPlanMode` before any code was written; a second-opinion review during
planning flagged three real risks — the AKS dual-formula ambiguity, the
`/chat` vs `/plan` idempotency contract, and treating `intake.py` as its
own flow controller rather than a 6th `SERVICE_FILES` entry — all three
incorporated into the plan before implementation started). Full design
recorded in `CLAUDE.md` → "Environment composer" and 5 new
`architecture-decisions.md` entries (LLM-never-computes-a-CIDR, the
AKS dual-formula resolution, the `snet_pe` override judgment call, the
intake-as-own-flow-controller call, and the no-new-table session-storage
decision).

- **Stage 1** (`2298611`): `advisor/composer/inventory_parser.py` +
  `network_planner.py`. Reproduced both `network_sizing.yaml`
  canonical examples exactly (arithmetic, subnet sizes, the
  strictly-greater-than-75% utilisation flag) before touching anything
  else, since this is the highest-risk module in the phase.
- **Stage 2** (`6fde2de`): `composition_engine.py` (8-phase
  `composition_rules.yaml` pipeline), `sequencer.py` (wave building +
  `RequestType.OTHER` label-mapping fix), `infosec.py` (gate + brief
  draft, recovering `public_hostname`/`audience` from the intake's one
  combined `public_details` field — a genuine KB-vs-question-bank
  mismatch found and fixed live). Caught and fixed a subtler bug during
  this stage: an inferred (never directly requested) Application Gateway
  wasn't being treated as "present" for `appgw_count`-gated warnings —
  fixed by threading an "effective answers" view through the
  warnings/deviations phases.
- **Stage 3** (`9da0fe1`): `intake.py` (the environment flow controller),
  `render.py` (deterministic markdown-shape renderer), `env_prefill.py`,
  `session_store.create_session(mode=...)`, `prompts
  .get_environment_system_prompt()`, and the 4 new
  `/api/advisor/environment/*` routes. Verified against the LIVE running
  dev server (not just the offline suite) for both canonical cases —
  found and fixed a real bug live: the subscription/sovereignty blocker
  was only checked once the whole intake finished, not after every
  answer like the single-service flow.
- **Stage 4** (`5144094`): `templates/advisor.html` mode picker +
  environment-mode plan renderer; `diagram_builder.render_environment()`
  + the `environment_full.mmd` "Azure CNI" → "Azure CNI Overlay" label
  fix (called out explicitly in the commit, not slipped in silently).
- **Stage 5** (`7e67cae`): extended `test_advisor_validation.py` to 232
  checks (up from 216) covering the remaining verification items —
  full render+diagram integration, arithmetic-integrity byte-for-byte
  comparisons, both canonical examples read directly from the YAML
  (not hand-copied), a forced-LLM-failure fallback check, and regression
  greps. Writing the diagram-negative check caught a real regex bug from
  Stage 4: three consecutive edge-stripping substitutions shared
  consumable newline boundaries, so only the first/last edge in a block
  actually got removed, leaving an orphan `AGW --> AKS` reference for an
  internal-only environment — fixed by switching to line-anchored
  `MULTILINE` matching, reverified live after the fix.
- **No headless browser available** in this environment (checked again
  this session: `npm ls puppeteer playwright` empty, no
  `chromium`/`google-chrome` binary) — stated explicitly rather than
  implied; UI verification was HTTP-round-trip + a `node --check` syntax
  pass on the extracted inline JS, not an actual visual click-through.
  See `.memory/next-actions.md` item 1 for the manual checklist.
