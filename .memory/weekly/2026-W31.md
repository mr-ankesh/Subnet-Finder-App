# Week 2026-W31 (Mon 2026-07-27 → Sun 2026-08-02)

> Daily tracking (`.memory/daily/`) starts 2026-08-01, so the first part of
> this week is reconstructed from `git log` rather than daily files.
> Update this file from daily entries as the week progresses.

## Major Accomplishments

- Approval flow shipped: line-manager routing (Entra `manager` claim →
  Keycloak → app), dependency-gated, with fallback-approver + super-admin
  fallback and self-approval blocked
- Keycloak-group-driven team visibility completed: teams enforced from SSO
  groups, individual-fallback messaging for zero-group users
- Rebrand copy pass: portal areas renamed (Request Center + Tickets), agent
  + copy broadened from "network" to general cloud ops language
- AKS: CMK/disk-encryption fixes (valid Key Vault/DES names, CMK off by
  default), live node OS SKU list, resource tags (project/owner/env/
  criticality/creator) across deploys
- Resource optimizer: CPU usage scan bug fixes (memory-metric 400 decoupled,
  real-error surfacing instead of silent empty results)
- Postgres portability fixes: `subinventory.ensure_table` PRAGMA crash on
  Postgres, `sqlite_to_postgres` migration script coverage gap (agent_chats,
  subscription_inventory, budget_alert_state) — both from the same root
  cause class (raw-SQL tables outside `models.py` need explicit backend
  handling)
- `CLAUDE.md` created — first checked-in architecture guide for future
  Claude sessions, including the local-vs-prod (SQLite/no-SSO vs
  Postgres/Keycloak/3-replica) distinction
- **2026-08-01:** `.memory/` persistent memory system implemented and
  populated (this file included)
- **2026-08-01:** VM(s) deployment feature committed (`65df6b9`) after
  partial local verification
- **2026-08-01:** Storage Account Request & Deploy feature
  (`RequestType.STORAGE_ACCOUNT_CREATE`) implemented end-to-end in 4 phases,
  matching the AKS/VM architecture — requester form, live Azure discovery
  (SKUs, identities, Key Vaults/keys), deploy engine (account → blob/file
  service properties → containers → best-effort replication/private
  endpoint), revert, extended tag schema, offline + live validation,
  assert-based test script. Verified via dry-run deploy in-browser.

## Important Decisions

- See `architecture-decisions.md` for full entries dated this week:
  line-manager approval routing (2026-07-30), rebrand steps
  (2026-07-26 → 2026-07-29), VM(s) deploy model (2026-07-31), and four
  Storage Account decisions (2026-08-01): tag-schema extension, delete-only
  rollback, reused discovery routes, and ledger "success" semantics.

## Risks

- Storage Account feature is sitting **uncommitted** in the working tree as
  of 2026-08-01 (see `current-state.md`) — dry-run verified only, no real
  Azure credentials available in this sandbox.
- `helm/subnet-manager/values.yaml` has an uncommitted prod-config change
  (`existingSecretName: "almadar-db"`) that needs confirmation against the
  real cluster before ship.
- Neither VM_CREATE nor STORAGE_ACCOUNT_CREATE has been verified against a
  real Azure subscription yet — both are dry-run/offline verified only.

## Carry-Forward Items

- Commit the Storage Account feature (P0 in `next-actions.md`)
- Full live-Azure verification of VM_CREATE + STORAGE_ACCOUNT_CREATE once
  real credentials are available
- Confirm Helm secret-name change is correct
- Resolve untracked `static/page-bg-original.jpg`
