apiVersion: v1
kind: Service
metadata:
  name: subnet-manager
  namespace: subnet-manager
spec:
  selector:
    app: subnet-manager
  ports:
    - name: http
      port: 80
      targetPort: 8080
  type: ClusterIP
