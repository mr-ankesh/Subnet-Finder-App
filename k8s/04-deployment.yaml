# IMPORTANT: Keep replicas: 1 — SQLite does not support concurrent writers.
# If you need HA, migrate to PostgreSQL and then scale up.
apiVersion: apps/v1
kind: Deployment
metadata:
  name: subnet-manager
  namespace: network-deployments
  labels:
    app: subnet-manager
spec:
  replicas: 1
  selector:
    matchLabels:
      app: subnet-manager
  strategy:
    type: Recreate          # Recreate (not RollingUpdate) for SQLite safety
  template:
    metadata:
      labels:
        app: subnet-manager
    spec:
      securityContext:
        runAsNonRoot: true
        runAsUser: 10001          # matches the 'app' user baked into the image
        runAsGroup: 10001
        fsGroup: 10001            # makes the PVC writable for the non-root user
        # Lets the non-root user run `ping` (kubectl exec) without CAP_NET_RAW —
        # enables unprivileged ICMP datagram sockets. A K8s "safe" sysctl.
        sysctls:
          - name: net.ipv4.ping_group_range
            value: "0 2147483647"
      containers:
        - name: subnet-manager
          # Replace with your ACR image: <acr-name>.azurecr.io/subnet-manager:<tag>
          image: <YOUR_ACR>.azurecr.io/subnet-manager:latest
          imagePullPolicy: Always
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop: ["ALL"]
          ports:
            - containerPort: 8080
          envFrom:
            - configMapRef:
                name: subnet-manager-config
            - secretRef:
                name: subnet-manager-secret
          volumeMounts:
            - name: data
              mountPath: /app/data
          livenessProbe:
            httpGet:
              path: /health
              port: 8080
            initialDelaySeconds: 15
            periodSeconds: 30
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /health
              port: 8080
            initialDelaySeconds: 10
            periodSeconds: 10
            failureThreshold: 3
          resources:
            requests:
              memory: "256Mi"
              cpu: "250m"
            limits:
              memory: "768Mi"
              cpu: "500m"
      volumes:
        - name: data
          persistentVolumeClaim:
            claimName: subnet-manager-data
      # Pull from ACR using AKS managed identity (attach ACR to AKS cluster):
      # az aks update -n <aks-name> -g <rg> --attach-acr <acr-name>
      # If using imagePullSecrets instead:
      # imagePullSecrets:
      #   - name: acr-secret
